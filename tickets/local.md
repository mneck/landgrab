# Local tickets (push to Linear)

Add tickets below. Run `node tickets/sync.mjs --push` to create them in Linear.

- Lines starting with `- ` or `- [ ] ` are parsed as new tickets.
- Optional: indent the next line(s) with 2+ spaces for a description.

---

<!-- Add tickets below, then run: node tickets/sync.mjs --push -->
